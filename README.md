# Основы DevOps и DataOps | ЛБ 4 | Развёртывание и администрирование Gitlab

**Дубровин Е.Н.**

МГТУ им. Н.Э. Баумана, 2024 г.

---

[TOC]

---

## Цель и задачи лабораторной работы

Цель лабораторной работы: Изучить основные концепции и инструменты Gitlab

Задачи лабораторной работы:

1. Развернуть собственную локальную версию Gitlab

2. Средствами администрирования создать пользователя и настроить доступность проектов

3. Создать проект, запланировать в нём работу, разместить в нём исходный текст программы, выполнить merge request

4. Добавить к проекту Gitlab Runner и скрипт CI/CD, собрать и запустить Docker образ с сохранением в Docker Registry

## Часть 1 | Развертывание Gitlab

Gitlab является одним из самых распространённых средств совмесного управления разработкой на основе git репозиториев.

Gitlab позиционируется как единая DevOps-платформа, в неё включено множество функциональных модулей:

* Управление Git репозиториями
* Управление запросами на слияние (merge requests)
* Управление пользователями и разграничение прав на доступ к репозиториям
* Пакетный и Docker реестры
* Управление CI/CD пайплайнами, интеграция с k8s кластером
* Создание статических сайтов Pages
* Управление задачами/проблемами Issue
* Возможность создавать снипеты кода
* Управление Вики проекта
* Автоматический вызов средств защиты ПО (SAST/DAST анализ)
* Мониторинг приложения
* Аналитика всех перечисленных выше модулей

Gitlab является свободным ПО, однако собранная версия приложения доступна в двух вариантах:

* Community Edition (ce) - бесплатная версия
* Enterprise Edition (ee) - платная версия, содержит расширанный функционал, который бесплатно можно получить только путём ручной сборки из исходного кода

Gitlab является комплесом программ, что значит, что его запуск в общем случае достаточно сложный просс - требуется установить, настроить и запустить множество программ.

Установка возможна как непосредственно в ОС, Docker или k8s.

Самым удобным способом установки является установка all-in-one Docker контейнера, в настоящей лабораторной работы использован именно этот способ.

All-in-one контейнер является контейнером, который не соответствует принципу "один контейнер - один процесс". Такие контейнеры включают в себя множество отдельных программ и множество запукаемых процессов.

Как правило, такой подход используется для запуска комплекса созависимых программ, которые нецелесообразно запускать отдельными контейнерами.

В конечном итоге, для запуска потребуется лишь указать переменные окружения, сетевые доступы и место хранение данных.

Поставка Gitlab в виде такого контейнера значительно упрощает запуск. Конфиуграционные параметры позволяют отключать не используемые в данный момент модули.

### Задание 1 | Локальная установка Gitlab

Установим Gitlab на локальном компьютере для знакомства с основными процессами его работы.

Для начала, следует запустить процесс выгрузки образа, образ является весьма объёмным (ок. 3GB).

```bash
docker pull gitlab/gitlab-ce:17.4.4-ce.0
```

🚨 В случае доступности tar архива образа, его можно импортировать при помощи команды:

```bash
docker load -i gitlab-ce_17.4.4-ce.0.tar
docker image ls
```

Перед началом запуска сгенерируем самопдписанные сертификаты:

```bash
openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -sha256 -days 365 -nodes -subj "/CN=gitlab.local" -addext "subjectAltName = DNS:gitlab.local"
```

Саму установку выполним при помощи Docker Compose, создадим файл `docker-compose.yaml`:

```yaml
services:
  gitlab:
    image: gitlab/gitlab-ce:17.4.4-ce.0
    ports:
      - mode: host
        protocol:  tcp
        published: 8022
        target:    22
      - mode: host
        protocol:  tcp
        published: 8443
        target:    8443
      - mode: host
        protocol:  tcp
        published: 8050
        target:    8050
    volumes:
      - gitlab-config:/etc/gitlab
      - gitlab-logs:/var/log/gitlab
      - gitlab-data:/var/opt/gitlab
    secrets:
      - source: cert-pem
        target: cert.pem
      - source: key-pem
        target: key.pem
    environment:
      GITLAB_OMNIBUS_CONFIG: |
          gitlab_rails['time_zone'] = 'Europe/Moscow'
          
          external_url 'https://gitlab.local:8443'
          gitlab_rails['gitlab_shell_ssh_port'] = 8022
          registry_external_url 'https://gitlab.local:8050'
          
          nginx['redirect_http_to_https'] = true
          registry_nginx['redirect_http_to_https'] = true

          letsencrypt['enable'] = false
          nginx['ssl_certificate']     = "/run/secrets/cert.pem"
          nginx['ssl_certificate_key'] = "/run/secrets/key.pem"

          registry_nginx['ssl_certificate']     = "/run/secrets/cert.pem"
          registry_nginx['ssl_certificate_key'] = "/run/secrets/key.pem"

          registry['enable'] = true
    deploy:
      resources:
        limits:
          cpus:   "15"
          memory: "15G"
        reservations:
          cpus:   "2"
          memory: "2G"

secrets:
  cert-pem:
    file: ./cert.pem
  key-pem:
    file: ./key.pem

volumes:
  gitlab-config:
  gitlab-logs:
  gitlab-data:

```

Перед запуском следует внести доменное имя контейнера в файл `/etc/hosts`, например:

```txt
127.0.0.1   localhost gitlab.local
```

Запустим Gitlab:

```bash
docker compose up
```

В другой консоли мы можем следить за состоянием контейнера поскольку для Gitlab прописан Healthcheck:

```bash
docker ps
```

После получения подтверждения о готовности контейнера, перейдём к Gitlab по ссылке [https://gitlab.local:8443](https://gitlab.local:8443).

По-умолчанию, пароль суперпользователя `root` при инциализации доступен только из внутренного хранилища Gitlab:

```bash
docker exec -it `docker compose ps -q` bash -c "cat /etc/gitlab/initial_root_password"
```

Стоит изменить пароль на собственный [https://gitlab.local:8443/-/user_settings/password/edit](https://gitlab.local:8443/-/user_settings/password/edit).

## Часть 2 | Работа в Gitlab

Gitlab имеет развитую систему администрирования и настроек, которая позволят управлять пользователями, доступами пользователей к проектам и парамтры создаваемых проектов.

Воспользуемся средствами администрирования, а также испольуем режим работы пользователя в Gitlab.

### Задание 2 | Администрирование Gitlab

Перейдём во вкладку администрирования [https://gitlab.local:8443/admin](https://gitlab.local:8443/admin).

Для пользователя администратора будут отображаться баннеры, среди которых будет `Check your sign-up restrictions`. Допустим, что инсталяция Gitlab предназначена для использования в закрытом контуре, а значит, следует отключить регистрацию пользователей, нажмите `Deactivate`.

Это перенесёт в настройки Gitlab в набор основных параметров. Отключим пункт `Sign-up enabled`, далее кнопку `Save changes` ниже.

Таким образом, новых пользователей потребуется добавлять администратору. Перейдём к пункту бокового меню `Overview -> Users`. Нажмём кнопку `New user`. Далее, следует указать собственную информацию, которая будет использована при входе в систему (ФИО, имя учётной записи и адрес электронной почты). Стоит указать адрес электронной почты, соответствующий адресу, прописанному в настройках git, по нему Gitlab сможет отделить изменения, которые вносит именно этот пользователь.

Далее, в карточке пользователя следует нажать кнопку `Edit`, указать пароль и его повтор для этого пользователя.

Обычно, для администрирования и нормальной работы в Gitlab применяетя одна и та же учётная запись, учётная запись root просто удаляется, а пользовательская учётная запись добавляется как администратор. Однако, в нашем случае, для режима тестовой работы в Gitlab это не целесообразно.

Далее, ограничим видимость проектов, которые могут создавать пользователи. Перейдём во вкладку `Settings -> General` и в поиске найдём `Restricted visibility levels`, такой способ поиска настроек с учётом их количества является наиболее удобным для пользователя-администратора системы.

После выполения поиска автоматически будет отображены настройки `Visibility and access controls`, найдём искомый пункт и запретим использовать тип видимости `Public` и `Internal`, это не позволит пользователям создавать публичные проекты и проекты, доступные только внутренним пльзователям Gitlab.

Выйдем из учётной записи администратора Gitlab. Для этого следует нажать на аватар пользователя и нажать на кнопку `Sign Out`.

### Задание 3 | Использование Gitlab

Выполним вход в учётную запись пользователя. После ввода пароля, указанного администратором, его потребуется сменить и повторить вход с новым паролем.

После входа добавим публичный ключ ssh для доступа к репозиториям по протоколу ssh [https://gitlab.local:8443/-/user_settings/ssh_keys](https://gitlab.local:8443/-/user_settings/ssh_keys).

Получить собственный ключ ssh можно при помощи:

```bash
# Получение публичной части ключа (может быть сгенерирован как rsa256, так и ed25519 ключ)
cat ~/.ssh/id_*.pub

# В случае если нет уже подготовленного ключа следует сгенерировать пару ключей
# Выполняйте только в случае если не выполняле ранее
# 🚨 ВНИМАНИЕ: может стереть предыдущие ключи пользователя!!!
ssh-keygen
```

Общая работа над проектами в Gitlab удобно может быть вынесена в отдельные пространства, группы. Создадим группу, для этого на домашней странице нажмём на кнопку `Create a group`.

название и путь может быть любым, стоит обратить внимание, что латинские символы и цифры автоматически превращаются в идентификатор группы.

Пригласим пользователя-администратора в группу как разработчика, для этого в боковой панели выберем пункт `Manage -> Members -> Invite Members`. Уровень доступа укажем `Developer`.

(После шага с созданием проекта можно попрбовать перезайти в учётную запись администратора и убедиться, что проект для этого пользователя отображается также как и для основного пользователя).

Создадим проект в группе, нажмём на кнопку `Create new project` на домашней странице группы. Следует создать пустой проект `Create blank project`. Далее, вводим название проека. Оставим галочку `Initialize repository with a README` по-умолчанию. Это сгенерирует стандартный проект с вводной информацией.

Начнём работу с проектом с создания задачи Issue, перейдём в пункт `Issue` и создадим новую задачу `New Issue` - тема `Подготовка начальной версии проекта`.

Далее, вернёмся на домашнюю страницу проекта и начнём работу с проектом во встроенном редакторе Gitlab. Следует нажать на кнопку `Edit -> Web IDE`.

В редакторе изменим файл `README.md`:

```markdown
# Тестовый проект

Представляет собой небольшой проект на bash, который выводит раз в 10 секунд сообщение о том, что он продолжает свою работу

## Запуск

`bash script.sh`

```

Также создадим файл `script.sh`:

```bash
#!/bin/sh

while true; do
    echo "Still working `date '+%d.%m.%Y %H:%M:%S'`"
    sleep 1
done

```

Далее, перейдём в режим коммита (иконка с развертвлением) и опишем следующее сообщение коммита:

```txt
Начало работы над проектом
 - Создана первая версия скрипта
 - Модифицирована документация

closes #1
```

Такое коммит-сообщение автоматически закроет Issue 1. Такой способ управления изменения удобен для совместной работы.

Не закрывая редактор, перейдите ко списку зкарытых задач Isses (`Issues -> Closed`) и убедитесь что задача #1 была закрыта автоматически.

**В отчёте следует отметить сообщение о закрытии задачи.**

Вернёмся к домашней странице проекта и получим его исходный текст по протоколу ssh. Для этого следует нажать на кнопку `Code` и скопировать путь до проекта в пункте `Clone with ssh`.

Получим исходный текст на рабочей машине при помощи `git clone`, пример команды ниже:

```bash
git clone ssh://git@gitlab.local:8022/first-group/first-project.git
```

Зайдём в полученную директорию и перейдём в ветку `dockerization`:

```bash
cd first-project

git checkout -b dockerization
```

Далее, добавим строки в `README.md`:

```markdwon
## Сборка контейнера

`docker build . -t image:tag`
```

И создадим `Dockerfile`:

```Dockerfile
FROM alpine:3.20

COPY script.sh /
RUN  chmod +x /script.sh

CMD ["/script.sh"]

```

Сделаем коммит изменений и предадим изменения в репозиторий:

```bash
git add .

git commit -m "Добавлен Dockerfile"

git push origin dockerization
```

Следует заметить сообщение с предложением создания merge request.

Однако, сделаем пример интереснее, введём параллельные изменения в основной ветке при помощи Web IDE в ветке main.

Добавим в файле `README.md` текст:

```markdown
## Развёртывание

Развёртывание выполняется автоматически при помощи Gitlab CI скрипта
```

И выполним коммит.

Теперь, создадим Merge Request, следует нажать на кнопку `Merge Requests`. Создадим новый merge Request и в качестве Source brnach следует указать ветку `dockerization`, а в качестве Target - `main`.

Далее, следует нажать кнопку `Compare branches and continue`. Следует снять чекбокс `Delete source branch when merge request is accepted.`, нам понадобится сохранить исходную ветку для отслеживания результатов работы. Нажмём `Create merge request`.

В нашем примере будет конфликт слияния, следуте их разрешить, сделаем это во встронном редакторе Gitlab, следует нажать на кнопку `Resolve conflicts`.

Следует сохранить все изменения, поэтому выйдем из интерактивного режима `Edit inline` и удалим все сервисные строки.

После получения сообщения о готовности к слиянию нажмём кнопку `Merge`.

Далее, перейдём во вкладку графа коммитов `Code -> Repository graph`. Здесь отобразится последовательность коммитов репозитория проекта.

**В отчётсе следует отобразить сстояние графа коммитов.**

## Часть 3 | Gitlab CI

Gitlab CI - это специальный формат файлов, главную часть которой состовляют bash срипты, которые позволяют выполнять множество разных действий:

* Выполнять сборку проекта
* Выполнять тестирование и проверку безопасности
* Выполнять развёртывание проекта
* Уведомлять о ходе работ проекта

Gitlab CI скрипты выполняются при помощи исполнителя Gitlab Runner. Gitlab Runner мжет иметь различные настройки окружения исполнения скриптов:

* SSH (подключение к удалённой машине)
* Shell (выполнение на локальной машине)
* Parallels
* VirtualBox
* Docker
* Docker Autoscaler
* Docker Machine (auto-scaling)
* Kubernetes
* Instance
* Custom

### Задание 4 | Использование Gitlab CI

Для начала работы скачаем исполняемый файл Gitlab Runner:

```bash
curl -O https://gitlab.com/gitlab-org/gitlab-runner/-/releases/v17.5.4/downloads/binaries/gitlab-runner-linux-amd64

chmod +x gitlab-runner-linux-amd64

./gitlab-runner-linux-amd64 --version
```

Зарегистрируем Gitlab Runner - для этого перейдём в настройки проекта `Settings -> CI/CD -> Runners`, следует нажать на кнопку `New Project Runner`.

В качестве тега введём `local` - это тег, который неоюходимо будет указать для запуска скрипта именно на этом Gitlab Runner.

Далее, скопируем только полученный токен подключения, особенности инсталяции Gitlab в нашем случае не позволяют воспользоваться подсказками, показываемыми на странице.

```bash
./gitlab-runner-linux-amd64 register --url https://gitlab.local:8443 --token <TOKEN> --tls-ca-file ./cert.pem
```

В качестве исполнителя (Executor) используем shell поскольку будем использовать локлаьные ресурсы для создания и развёртывания целевого образа.

Далее, запустим Gitlab Runner

```bash
./gitlab-runner-linux-amd64 run
```

Любым из удобных способов добавьте в проект файл `.gitlab-ci.yml`. Например, это можно сделать при помощи внутреннего средства разработки Gitlab CI `Build -> Pipeline editor`.

```yaml
stages:          # Список стадий CI скрипта проекта и порядок их выполнения
  - build
  - test
  - deploy

build-job:       # Эта задача (job) выполняется на стадии сборки, выполняется первой
  stage: build
  script:
    - echo "Компиляция..."
    - echo "Компиляция завершена"
  tags:
    - local

unit-test-job:   # Эта задача выполняется на стадии тестирования
  stage: test    # Она запускается только в случае если сборка прошла успешно
  script:
    - echo "Запуск unit-тестирования... Это займёт около 60 секунд"
    - sleep 60
    - echo "Покрытие кода 90%"
  tags:
    - local

lint-test-job:   # Эта задача также выполняется на стадии тестирования
  stage: test    # Она может запуститься параллельно задаче выше
  script:
    - echo "Проверка форматирования кода... Это займёт около 10 секунд"
    - sleep 10
    - echo "Проблем форматирования кода нет"
  tags:
    - local

deploy-job:      # Эта задача запускается на стадии деплоя
  stage: deploy  # Она зпусится только в случае если *обе* предыдущие стадии выполнятся успешно
  environment: production
  script:
    - echo "Запуск приложения..."
    - echo "Приложение запущено успешно!"
  tags:
    - local
```

Перейдём во вкладку с выполненными CI-последовательностяими (Pipeline) `Build -> Pipelines`. Здесь отображаются статусы выполнения стадий, описанных в CI скрипте. Если перейти в конкретный pipilene, то возможно увидеть результаты выполнения и логи работы всех задач. Примерно через 60 секунд после запуска все задачи и весь pipeline целиком завершится.

**В отчёте следует зафиксировать состояние выполненного Pipeline.**

Для продуктивных решений обычно применяют Docker executor поскольку он явно разделяет все задачи (job) между собой. Также это позволяет использовать отдельные образы под каждую из задач, что позволяет использовать только нужные инструменты в процессе работы.

Однако, в настоящей лабораторной работе сборка и развёртывания приложения произойдёт на той же машине что и сборка - локальном коммпьютере.

Опишем процесс сборки образа, его публикации и запуска в рамках `.gitlab-ci.yml` сценария:

```yaml
stages:
  - build
  - deploy

build-job:
  stage: build
  script:
    - echo "Вход в реестр Docker..."
    - docker login $CI_REGISTRY --username $CI_REGISTRY_USER --password $CI_REGISTRY_PASSWORD
    
    - echo "Сборка приложения..."
    - docker build . --push -t $CI_REGISTRY_IMAGE
    
    - echo "Очистка регистрационных данных реестра Docker..."
    - docker logout

    - echo "Сборка завершена успешно!"
  tags:
    - local

deploy-job:
  stage: deploy
  environment: production
  script:
    - echo "Запуск приложения..."
    - docker run -dt $CI_REGISTRY_IMAGE
    - echo "Приложение запущено успешно!"
  tags:
    - local

```

**В отчёте следует указать логи выполнения всех задач в рамках последнего pipeline.**

Убедимся в том, что образ был создан в Docker реестре Gitlab: `Deploy -> Container Registry`.

**В отчёте следует указать содержимое Docker реестра Gitlab.**

Также убедимся в том, что приложение успешно запущено:

```bash
docker ps
```

**В отчёте следует указать и пояснить чем являются запущенные контейнеры.**

## Очистка после выполнения лабораторной работы

Следует остановить и удалить запущенный в рамках CI скрипта контейнер (см. ЛБ2).

Также остановим Gitlab:

```bash
docker compose stop
docker compose rm
```

## Содержание отчёта

1. [`Цель и задачи лабораторной работы`](#цель-и-задачи-лабораторной-работы)
2. [`Задание 1 | Локальная установка Gitlab`](#задание-1--локальная-установка-gitlab)
3. [`Задание 2 | Администрирование Gitlab`](#задание-2--администрирование-gitlab)
4. [`Задание 3 | Использование Gitlab`](#задание-3--использование-gitlab)
5. [`Задание 4 | Использование Gitlab CI`](#задание-4--использование-gitlab-ci)
